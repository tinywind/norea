diff --git a/openvpn/client/cliopt.hpp b/openvpn/client/cliopt.hpp
index c534c46..6f0663d 100644
--- a/openvpn/client/cliopt.hpp
+++ b/openvpn/client/cliopt.hpp
@@ -1358,10 +1358,11 @@ class ClientOptions : public RC<thread_unsafe_refcount>
         transconf.server_addr_float = server_addr_float;
         transconf.synchronous_dns_lookup = synchronous_dns_lookup;
         transconf.protocol = transport_protocol;
-        transport_factory = extern_transport_factory->new_transport_factory(transconf);
 #ifdef OPENVPN_GREMLIN
-        udpconf->gremlin_config = gremlin_config;
+        if (gremlin_config)
+            transconf.gremlin_config = gremlin_config->to_string();
 #endif
+        transport_factory = extern_transport_factory->new_transport_factory(transconf);
 
 #else
         if (dco)
diff --git a/openvpn/transport/client/extern/config.hpp b/openvpn/transport/client/extern/config.hpp
index 459ffd3..aa606e8 100644
--- a/openvpn/transport/client/extern/config.hpp
+++ b/openvpn/transport/client/extern/config.hpp
@@ -24,6 +24,7 @@ struct Config
     RemoteList::Ptr remote_list;
     bool server_addr_float = false;
     bool synchronous_dns_lookup = false;
+    std::string gremlin_config;
     Frame::Ptr frame;
     SessionStats::Ptr stats;
     SocketProtect *socket_protect = nullptr;
diff --git a/openvpn/transport/client/extern/fw.hpp b/openvpn/transport/client/extern/fw.hpp
index 1799de1..770958a 100644
--- a/openvpn/transport/client/extern/fw.hpp
+++ b/openvpn/transport/client/extern/fw.hpp
@@ -12,22 +12,24 @@
 #ifndef OPENVPN_TRANSPORT_CLIENT_EXTERN_FW_H
 #define OPENVPN_TRANSPORT_CLIENT_EXTERN_FW_H
 
+namespace openvpn {
 #ifdef OPENVPN_EXTERNAL_TRANSPORT_FACTORY
-#include <openvpn/transport/client/transbase.hpp>
-#endif
+struct TransportClientFactory;
 
-namespace openvpn::ExternalTransport {
-#ifdef OPENVPN_EXTERNAL_TRANSPORT_FACTORY
+namespace ExternalTransport {
 struct Config;
 struct Factory
 {
     virtual TransportClientFactory *new_transport_factory(const Config &conf) = 0;
     virtual ~Factory() = default;
 };
+} // namespace ExternalTransport
 #else
+namespace ExternalTransport {
 struct Factory
 {
 };
+} // namespace ExternalTransport
 #endif
-} // namespace openvpn::ExternalTransport
+} // namespace openvpn
 #endif
diff --git a/openvpn/transport/gremlin.hpp b/openvpn/transport/gremlin.hpp
index b1d1d30..cba22d4 100644
--- a/openvpn/transport/gremlin.hpp
+++ b/openvpn/transport/gremlin.hpp
@@ -17,6 +17,7 @@
 #include <vector>
 #include <utility>
 #include <sstream>
+#include <type_traits>
 
 #include <openvpn/common/rc.hpp>
 #include <openvpn/common/string.hpp>
@@ -44,8 +45,9 @@ struct DelayedQueue : public RC<thread_unsafe_refcount>
     template <class F>
     void queue(F &&func_arg)
     {
+        using Fn = std::decay_t<F>;
         const bool empty = events.empty();
-        events.emplace_back(new Event<F>(Time::now() + dur, std::move(func_arg)));
+        events.emplace_back(new Event<Fn>(Time::now() + dur, std::forward<F>(func_arg)));
         if (empty)
             set_timer();
     }
@@ -72,9 +74,10 @@ struct DelayedQueue : public RC<thread_unsafe_refcount>
     struct Event : public EventBase
     {
       public:
-        Event(Time fire_arg, F &&func_arg)
+        template <class G>
+        Event(Time fire_arg, G &&func_arg)
             : fire(fire_arg),
-              func(std::move(func_arg))
+              func(std::forward<G>(func_arg))
         {
         }
 
diff --git a/openvpn/transport/tcplinkbase.hpp b/openvpn/transport/tcplinkbase.hpp
index 7646b08..affc19b 100644
--- a/openvpn/transport/tcplinkbase.hpp
+++ b/openvpn/transport/tcplinkbase.hpp
@@ -7,6 +7,10 @@
 #include <openvpn/buffer/buffer.hpp>
 #include <openvpn/common/rc.hpp>
 
+#ifdef OPENVPN_GREMLIN
+#include <openvpn/transport/gremlin.hpp>
+#endif
+
 #pragma once
 
 namespace openvpn::TCPTransport {
@@ -31,6 +35,9 @@ class LinkBase : public RC<thread_unsafe_refcount>
     virtual void reset_align_adjust(const size_t align_adjust) = 0;
     virtual bool send(BufferAllocated &b) = 0;
     virtual void set_raw_mode(const bool mode) = 0;
+#ifdef OPENVPN_GREMLIN
+    virtual void gremlin_config(const Gremlin::Config::Ptr &config) = 0;
+#endif
     virtual void start() = 0;
     virtual void stop() = 0;
 };
diff --git a/openvpn/transport/tcplinkcommon.hpp b/openvpn/transport/tcplinkcommon.hpp
index 2aed09b..5cb0ac0 100644
--- a/openvpn/transport/tcplinkcommon.hpp
+++ b/openvpn/transport/tcplinkcommon.hpp
@@ -238,7 +238,10 @@ class LinkCommon : public LinkBase
     void gremlin_config(const Gremlin::Config::Ptr &config)
     {
         if (config)
-            gremlin.reset(new Gremlin::SendRecvQueue(socket.get_executor().context(), config, true));
+            gremlin.reset(new Gremlin::SendRecvQueue(
+                static_cast<openvpn_io::io_context &>(socket.get_executor().context()),
+                config,
+                true));
     }
 #endif
 
diff --git a/openvpn/transport/udplink.hpp b/openvpn/transport/udplink.hpp
index ea0cfc9..b39d6da 100644
--- a/openvpn/transport/udplink.hpp
+++ b/openvpn/transport/udplink.hpp
@@ -78,7 +78,10 @@ class UDPLink : public RC<thread_unsafe_refcount>
     void gremlin_config(const Gremlin::Config::Ptr &config)
     {
         if (config)
-            gremlin.reset(new Gremlin::SendRecvQueue(socket.get_executor().context(), config, false));
+            gremlin.reset(new Gremlin::SendRecvQueue(
+                static_cast<openvpn_io::io_context &>(socket.get_executor().context()),
+                config,
+                false));
     }
 #endif
 
@@ -209,10 +212,10 @@ class UDPLink : public RC<thread_unsafe_refcount>
         std::unique_ptr<AsioEndpoint> ep;
         if (endpoint)
             ep.reset(new AsioEndpoint(*endpoint));
-        gremlin->send_queue([self = Ptr(this), buf = BufferAllocatedRc(buf, 0), ep = std::move(ep)]() mutable
+        gremlin->send_queue([self = Ptr(this), buf = BufferAllocatedRc::Create(buf, 0), ep = std::move(ep)]() mutable
                             {
 	    if (!self->halt)
-	      self->do_send(buf, ep.get()); });
+	      self->do_send(*buf, ep.get()); });
     }
 
     void gremlin_recv(PacketFrom::SPtr &pfp)
diff --git a/openvpn/client/cliconnect.hpp b/openvpn/client/cliconnect.hpp
index 4886440..e7bd802 100644
--- a/openvpn/client/cliconnect.hpp
+++ b/openvpn/client/cliconnect.hpp
@@ -283,2 +283,13 @@ class ClientConnect : public RC<thread_unsafe_refcount>
             client->start_acc_certcheck(std::forward<ArgsT>(args)...);
     }
+    void thread_safe_start_acc_certcheck(SSLLib::SSLAPI::Config::Ptr config)
+    {
+        if (!halt)
+            openvpn_io::post(io_context,
+                             [self = Ptr(this), config = std::move(config)]() mutable
+                             {
+                                 OPENVPN_ASYNC_HANDLER;
+                                 self->start_acc_certcheck(std::move(config));
+                             });
+    }
+
diff --git a/client/ovpncli.cpp b/client/ovpncli.cpp
index fb1c8f3..34f6d07 100644
--- a/client/ovpncli.cpp
+++ b/client/ovpncli.cpp
@@ -1362,1 +1362,1 @@ OPENVPN_CLIENT_EXPORT void OpenVPNClient::start_cert_check(const std::string &cli
-            session->start_acc_certcheck(config);
+            session->thread_safe_start_acc_certcheck(config);
@@ -1389,1 +1389,1 @@ OPENVPN_CLIENT_EXPORT void OpenVPNClient::start_cert_check_epki(const std::string
-            session->start_acc_certcheck(config);
+            session->thread_safe_start_acc_certcheck(config);
diff --git a/openvpn/client/async_resolve.hpp b/openvpn/client/async_resolve.hpp
index 20827eb..637ae10 100644
--- a/openvpn/client/async_resolve.hpp
+++ b/openvpn/client/async_resolve.hpp
@@ -14,0 +14,2 @@
+
+#include <rust_resolver.hpp>
@@ -23,2 +25,2 @@
-typedef AsyncResolvable<openvpn_io::ip::udp::resolver> AsyncResolvableUDP;
-typedef AsyncResolvable<openvpn_io::ip::tcp::resolver> AsyncResolvableTCP;
+typedef AsyncResolvable<RustResolver<openvpn_io::ip::udp>> AsyncResolvableUDP;
+typedef AsyncResolvable<RustResolver<openvpn_io::ip::tcp>> AsyncResolvableTCP;
diff --git a/openvpn/crypto/cryptochoose.hpp b/openvpn/crypto/cryptochoose.hpp
index 28c9d58..885e571 100644
--- a/openvpn/crypto/cryptochoose.hpp
+++ b/openvpn/crypto/cryptochoose.hpp
@@ -17,0 +17,4 @@
+#ifdef USE_RUST_CRYPTO
+#include <rust_crypto.hpp>
+#endif
+
@@ -43,1 +47,5 @@
-#if defined(USE_MBEDTLS)
+#if defined(USE_RUST_CRYPTO)
+#define SSL_LIB_NAME "RustCrypto"
+using CryptoAPI = RustCryptoAPI;
+using RandomAPI = RustRandom;
+#elif defined(USE_MBEDTLS)
diff --git a/openvpn/ssl/sslchoose.hpp b/openvpn/ssl/sslchoose.hpp
index 267efbd..798de28 100644
--- a/openvpn/ssl/sslchoose.hpp
+++ b/openvpn/ssl/sslchoose.hpp
@@ -18,0 +18,4 @@
+#ifdef USE_RUST_CRYPTO
+#include <rust_tls.hpp>
+#endif
+
@@ -18,1 +22,1 @@
-#ifdef USE_OPENSSL
+#if defined(USE_OPENSSL) && !defined(USE_RUST_CRYPTO)
@@ -42,1 +46,6 @@
-#if defined(USE_MBEDTLS)
+#if defined(USE_RUST_CRYPTO)
+#define SSL_LIB_NAME "RustTLS"
+using SSLAPI = RustTLSContext;
+using PEMAPI = RustPEM;
+#elif defined(USE_MBEDTLS)
diff --git a/openvpn/transport/client/udpcli.hpp b/openvpn/transport/client/udpcli.hpp
index a271887..3ccb10a 100644
--- a/openvpn/transport/client/udpcli.hpp
+++ b/openvpn/transport/client/udpcli.hpp
@@ -335,7 +335,7 @@ class Client : public TransportClient, AsyncResolvableUDP
     ClientConfig::Ptr config;
     TransportClientParent *parent;
     LinkImpl::Ptr impl;
-    openvpn_io::ip::udp::resolver resolver;
+    AsyncResolvableUDP::resolver_type resolver;
     UDPTransport::AsioEndpoint server_endpoint;
     bool halt;
 };
diff --git a/openvpn/transport/client/tcpcli.hpp b/openvpn/transport/client/tcpcli.hpp
index e8044ea..721e97b 100644
--- a/openvpn/transport/client/tcpcli.hpp
+++ b/openvpn/transport/client/tcpcli.hpp
@@ -388,7 +388,7 @@ class Client : public TransportClient, AsyncResolvableTCP
     ClientConfig::Ptr config;
     TransportClientParent *parent;
     LinkBase::Ptr impl;
-    openvpn_io::ip::tcp::resolver resolver;
+    AsyncResolvableTCP::resolver_type resolver;
     LinkImpl::Base::protocol::endpoint server_endpoint;
     bool halt;
     bool stop_requeueing;
